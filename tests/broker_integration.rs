//use brokust::broker::Broker; // Adjust the import path as per your crate name and module structure
//
//#[tokio::test]
//async fn test_broker_initialization() {
//    let broker = Broker::new();
//    assert_eq!(broker.uuid.is_nil(), false);
//}
